export class PositionTracker {
    position: number;
    count: number;

    constructor(position: number, count: number) {
        this.position = position;
        this.count = count;
    }
}