export class GridLocation {
    x: number;
    y: number;
    reading: string;
    constructor(x: number, y:number, reading: string){
        this.x = x;
        this.y= y;
        this.reading = reading;
    }
}